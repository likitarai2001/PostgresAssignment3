server:
  port: 9090

#Datasource configuration
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/online_shopping2
    username: postgres
    password: Simform@123
    driver-class-name: org.postgresql.Driver

#Hibernate configuration
  jpa:
    properties:
      hibernate:
        format_sql: true
    show-sql: true
    hibernate:
      ddl-auto: create-drop
